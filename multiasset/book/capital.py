# -*- coding: utf-8 -*-
"""Long-only, unlevered capital constraint with a utilisation buffer.

Reserved for Step 7 of docs/plans/beta_book_exposure_vs_capital.md:

    def weights_to_notional(weights, total_capital, max_utilisation=0.95,
                            signed_classes=('FX',)) -> pd.Series: ...

No logic yet — this module is a placeholder created in Step 1 (pure file
split) so later steps are additions, not new-file churn.
"""
