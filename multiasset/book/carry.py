# -*- coding: utf-8 -*-
"""Reserved. Per the plan (docs/plans/beta_book_exposure_vs_capital.md Step 5),
carry math is implemented inside pnl.py's compute_book_pnl() rather than as a
standalone module — this file exists so the package layout matches the plan
document's table exactly and stays a stable reference point; it may remain
empty if carry never needs its own module separate from pnl.py.
"""
